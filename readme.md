## 🚀 Configuração do Ambiente

É **altamente recomendável criar um ambiente virtual** para o projeto,
evitando conflitos de dependências entre diferentes aplicações Python.

------------------------------------------------------------------------

## 🐧 Instalação no Linux (Ubuntu/Debian)

### 1. Instale o módulo de ambientes virtuais

``` bash
sudo apt install python3.12-venv
```

### 2. Crie o ambiente virtual

``` bash
python3 -m venv nome_do_ambiente
```

### 3. Ative o ambiente virtual

``` bash
source nome_do_ambiente/bin/activate
```

### 4. Instale as dependências

``` bash
pip install -r requirements.txt
```

### 5. Execute a aplicação

``` bash
python app.py
```

------------------------------------------------------------------------

## 🪟 Instalação no Windows

### 1. Verifique se o Python está instalado

``` powershell
python --version
```

### 2. Crie o ambiente virtual

``` powershell
python -m venv nome_do_ambiente
```

### 3. Ative o ambiente virtual

``` powershell
nome_do_ambiente\Scripts\activate
```

### 4. Instale as dependências

``` powershell
pip install -r requirements.txt
```

### 5. Execute a aplicação

``` powershell
python app.py
```

------------------------------------------------------------------------

## 🍎 Instalação no macOS

### 1. Instale o Python (se necessário)

Com Homebrew:

``` bash
brew install python
```

### 2. Crie o ambiente virtual

``` bash
python3 -m venv nome_do_ambiente
```

### 3. Ative o ambiente virtual

``` bash
source nome_do_ambiente/bin/activate
```

### 4. Instale as dependências

``` bash
pip install -r requirements.txt
```

### 5. Execute a aplicação

``` bash
python app.py
```
